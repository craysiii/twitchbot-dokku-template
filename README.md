# twitchbot-dokku-template

### How to deploy:
1. Create an application in dokku
2. Add `BOT_USERNAME`, `BOT_PASSWORD` (`oauth:xxxxx` format), and `BOT_CHANNEL` environment variables to application
3. Push project to dokku 